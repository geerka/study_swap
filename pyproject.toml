[tool.poetry]
name = "study-swap"
version = "1.0.0"
description = "StudySwap - Student materials marketplace"

[tool.poetry.dependencies]
python = "^3.11"
flask = "^3.0.0"
flask-sqlalchemy = "^3.1.0"
flask-login = "^0.6.3"
werkzeug = "^3.0.0"
gunicorn = "^21.0.0"
