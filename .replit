run = "pip install -r requirements.txt && python -m flask --app main run --host=0.0.0.0 --port=5000"
modules = ["python-3.11"]

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "pip install -r requirements.txt && python -m flask --app main run --host=0.0.0.0 --port=5000"]
